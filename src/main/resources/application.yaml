server:
  servlet:
    context-path: "/api"
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/db_example
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver